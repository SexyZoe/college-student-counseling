<!-- pages/index/index.wxml -->
<view class="container">
  <!-- 隐私提示横幅（首次展示） -->
  <view class="privacy-banner" wx:if="{{showPrivacy}}">
    <van-icon name="info-o" size="16px" color="#f4a3a8" />
    <text class="privacy-text">{{privacyNotice}}</text>
    <van-icon name="cross" size="14px" color="#999" bindtap="onClosePrivacy" />
  </view>

  <view class="task-section" wx:if="{{isLoggedIn}}">
    <view class="section-header"><text class="section-title">当前测评任务</text><text class="semester-text">{{currentSemester}}</text></view>
    <view class="task-card" wx:for="{{pendingTasks}}" wx:key="id" data-task-id="{{item.id}}" data-assessment-id="{{item.assessmentId}}" bindtap="onTaskTap">
      <view class="task-main"><text class="task-title">{{item.title}}</text><text class="task-deadline">截止：{{item.deadline}}</text></view><view class="task-action">去完成</view>
    </view>
    <view class="recent-result" wx:if="{{recentResult}}"><text>最近结果：{{recentResult.assessmentName}}</text><text class="recent-level">{{recentResult.level}}</text></view>
    <view class="empty-task" wx:if="{{pendingTasks.length === 0}}">当前没有待完成任务</view>
  </view>

  <!-- 顶部问候 + 班级信息 -->
  <view class="header-section">
    <view class="greeting-area">
      <view class="greeting-left">
        <text class="greeting-title">{{isLoggedIn ? '你好，' + userInfo.nickName : '你好，同学'}}</text>
        <text class="greeting-sub">今天心情如何？</text>
      </view>
      <view class="user-avatar" bindtap="{{isLoggedIn ? 'goMine' : 'goLogin'}}">
        <image class="avatar-img" src="{{isLoggedIn ? userInfo.avatarUrl : '/images/articles/article_growth.png'}}" mode="aspectFill" />
      </view>
    </view>
    <!-- 班级 & 辅导员信息（已登录+有学号时显示） -->
    <view class="class-info" wx:if="{{isLoggedIn && userInfo.studentId}}">
      <view class="class-info-item">
        <van-icon name="label-o" size="14px" color="#8fc8b5" />
        <text>{{userInfo.grade}} {{userInfo.major}} {{userInfo.className}}</text>
      </view>
      <view class="class-info-item" wx:if="{{userInfo.counselor}}">
        <van-icon name="friends-o" size="14px" color="#8fc1e0" />
        <text>辅导员：{{userInfo.counselor}}</text>
      </view>
      <view class="class-info-item">
        <van-icon name="orders-o" size="14px" color="#f5d79e" />
        <text>待完成测评：{{pendingAssessmentCount}}</text>
      </view>
    </view>
  </view>

  <!-- 轮播图 -->
  <view class="banner-section">
    <swiper class="banner-swiper" indicator-dots="{{true}}" autoplay="{{true}}" interval="{{4000}}" duration="{{500}}" circular="{{true}}" indicator-color="rgba(255,255,255,0.5)" indicator-active-color="#ffffff">
      <swiper-item wx:for="{{bannerList}}" wx:key="id" bindtap="onBannerTap" data-id="{{item.id}}">
        <image class="banner-image" src="{{item.image}}" mode="aspectFill" />
        <view class="banner-title">{{item.title}}</view>
      </swiper-item>
    </swiper>
  </view>

  <!-- 快捷服务入口 -->
  <view class="quick-entry-section">
    <view class="section-title">核心服务</view>
    <view class="entry-grid">
      <view class="entry-item" wx:for="{{quickEntries}}" wx:key="title" bindtap="onQuickEntryTap" data-path="{{item.path}}">
        <view class="entry-icon-wrapper" style="background-color: {{item.color}};">
          <van-icon name="{{item.icon}}" size="24px" color="#ffffff" />
        </view>
        <text class="entry-title">{{item.title}}</text>
      </view>
    </view>
  </view>

  <!-- 心理资讯 -->
  <view class="article-section" wx:if="{{civicsArticles.length}}">
    <view class="section-header"><text class="section-title">成长与价值</text><view class="section-more" bindtap="onQuickEntryTap" data-path="/pages/civics/index"><text class="more-text">更多</text><van-icon name="arrow" size="14px" color="#999" /></view></view>
    <view class="civics-mini" wx:for="{{civicsArticles}}" wx:key="id" data-id="{{item.id}}" bindtap="onCivicsTap"><view><text class="civics-mini-tag">{{item.category}}</text><text class="civics-mini-title">{{item.title}}</text><text class="civics-mini-summary">{{item.summary}}</text></view><van-icon name="arrow" size="16px" color="#bbb" /></view>
  </view>

  <!-- 心理资讯 -->
  <view class="article-section">
    <view class="section-header">
      <text class="section-title">每日心理资讯</text>
      <view class="section-more" bindtap="onQuickEntryTap" data-path="/pages/article/list">
        <text class="more-text">更多</text>
        <van-icon name="arrow" size="14px" color="#999" />
      </view>
    </view>
    <view class="article-list">
      <view class="article-card" wx:for="{{articles}}" wx:key="id" bindtap="onArticleTap" data-id="{{item.id}}">
        <image class="article-cover" src="{{item.cover}}" mode="aspectFill" />
        <view class="article-content">
          <view class="article-tag">{{item.category}}</view>
          <text class="article-title">{{item.title}}</text>
          <text class="article-summary">{{item.summary}}</text>
          <view class="article-meta">
            <van-icon name="eye-o" size="14px" color="#999" />
            <text class="meta-text">{{item.views}} 阅读</text>
          </view>
        </view>
      </view>
    </view>
  </view>

  <!-- 底部安全区 -->
  <view class="bottom-safe"></view>
</view>
