# Web 迁移验收记录

日期：2026-09-24。以下为本次实际执行结果，不代表学校已批准正式运营。

## 源码与运行入口

微信源码备份已推送并核实：`wechat-final-2026-09-24` → `fa2f90115ea2ba34de066095cda8a5453222f52d`。

Web 使用 `/web/`；学生、辅导员、管理员同源登录。当前运行源码已移除微信页面、组件包与配置。保留共享评分引擎和原业务数据库。

## 已执行验证

| 检查 | 结果与范围 |
|---|---|
| 评分单元测试 | 7项通过；反向题、阈值、关键题、缺题和版本快照 |
| Node后端测试 | 95项通过；包含原有业务及新Cookie、CSRF、114题评分一致性、同意留痕、幂等、媒体访问和下架 |
| Chrome浏览器 | 通过；390px手机视口和桌面视口 |
| WebKit浏览器 | 通过；与Chrome相同的真实界面/API流程，不等于iPhone实机验收 |
| 页面流程 | 学生登录、独立同意、刷新恢复、断网重试、结果/历史、人格探索、隐私/求助、首次姓名和改密 |
| 教师/管理流程 | 创建并发布任务、创建学期、CSV预检导入、学生支持摘要、风险跟进、文章投稿→审核→学生阅读、退出 |
| 导航稳定性 | 慢接口返回后不会覆盖用户已切换到的新页面 |
| 界面 | 检查手机首页/测评结果及桌面名单截图；无页面JS异常、手机首页无横向溢出 |
| Docker | 最终源代码镜像构建成功；非root、只读文件系统、最小权限参数运行 |
| MySQL 8.4 | 隔离容器完成7个历史迁移，业务契约通过，新增Cookie/8套网页量表提交与重试验证 |
| 依赖审计 | 根目录开发依赖及server依赖当次审计均无已知漏洞 |
| 配置 | 正式Compose组合通过解析；OpenAPI所有内部引用通过检查 |

浏览器截图位于本地忽略目录 `artifacts/`，CI也会保存截图作为构建附件。测试使用虚构数据和隔离数据库，没有向校园在线数据库写入测试数据。

## 本机性能样本

目标为本机Docker中的MySQL后端，未经过学校Wi-Fi，不据此推断全校并发能力。

| 场景 | 请求/并发 | 失败率 | P95 |
|---|---|---|---|
| 就绪检查 | 1,000 / 50 | 0% | 28.77 ms |
| 学生登录 | 100 / 10 | 0% | 195.36 ms |

就绪检查不代表完整测评提交负载。计划集中测评前仍需根据人数开展校园环境压测，并核实同出口IP的限流参数。

## 尚未完成的外部条件

- 校园实例通过指定Wi-Fi网卡可访问，但本次初次SSH免密尝试被拒绝；用户表示将配置密钥登录。
- 学校名称、心理中心电话/地点/时间、隐私联系渠道及保留期限尚未收到确认。
- 原量表/阈值仍是历史原型版本，未取得新的学校专业审核及授权记录。
- 正式HTTPS域名、手机信任证书、数据库TLS、真实网络边界、学校真机验收与风险响应安排需落实。
- 未将“本机验收通过”表述为“已获准正式服务真实学生”。

旧雷达图曾允许用总分模拟五维数据；网页不生成这种模拟结果，改为真实单量表指数和同量表历史趋势，详见迁移对照。
