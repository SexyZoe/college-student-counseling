# 安全政策

## 支持范围

项目处于校内试点开发阶段。只有`main`分支的最新版本接受安全修复；演示账号、虚构数据和本地SQLite环境不得用于生产。

## 报告漏洞

请不要在公开Issue中提交以下内容：真实学生信息、心理答题或风险记录、账号密码、访问令牌、数据库连接、学校API密钥、服务器地址及可直接利用的漏洞细节。

请通过学校项目负责人或信息中心指定的私密渠道报告，并提供：受影响版本、复现步骤、影响范围、日志中的`X-Request-Id`和建议修复方式。正式联系人由学校确定后补充，确定前不得导入真实数据。

项目组应在收到报告后先确认接收、隔离影响、保留审计证据，再修复和验证；未经授权不得在生产或学校网络进行扫描、压测或利用验证。

## 密钥泄露处理

若密钥进入Git、聊天或日志，不能只删除文件，应立即：

1. 撤销或轮换对应密钥；
2. 注销受影响会话和账号；
3. 检查审计日志和异常访问；
4. 评估是否涉及敏感个人信息并按学校流程上报；
5. 清理历史记录并确认新密钥只保存在密钥管理平台。
